# マインドマップエディタ VS Code拡張機能 受け入れテスト仕様書

**文書番号**: MME-AT-001  
**バージョン**: 1.6.0  
**作成日**: 2026-06-19  
**最終更新**: 2026-07-11  
**ステータス**: 承認済み  
**対象要求仕様書**: MME-REQ-001-USDM **v2.19.1**（2026-07-11）

> **バージョン対応表**
> 本テスト仕様書は要求仕様書 v2.19.1 に基づいて作成されています。
> 要求仕様書のバージョンが変更された場合は、本文書も対応するバージョンにアップデートしてください。

---

## テスト対象一覧

| 要求ID | 要求名 | テストID範囲 |
|--------|--------|-------------|
| R-01 | マインドマップ表示 | AT-01-01〜AT-01-09 |
| R-02 | ノード移動（ドラッグ＆ドロップ） | AT-02-01〜AT-02-09 |
| R-03 | ノード編集（インライン編集） | AT-03-01〜AT-03-04 |
| R-04 | ノード追加 | AT-04-01〜AT-04-03 |
| R-05 | ノード削除 | AT-05-01〜AT-05-03 |
| R-06 | 折りたたみ | AT-06-01〜AT-06-08 |
| R-07 | パン・ズーム | AT-07-01〜AT-07-05 |
| R-08 | ノード上下入れ替え | AT-08-01〜AT-08-03 |
| R-09 | ファイル保存（Ctrl+S） | AT-09-01〜AT-09-02 |
| R-10 | Undo | AT-10-01〜AT-10-04 |
| R-11 | 双方向同期の競合管理 | AT-11-01〜AT-11-08 |
| R-12 | キーボードナビゲーション | AT-12-01〜AT-12-13 |
| R-13 | 本文項目のマインドマップツリー表示 | AT-13-01〜AT-13-18 |
| R-14 | 本文項目⇔見出しノードの変換 | AT-14-01〜AT-14-06 |
| R-15 | 本文項目の階層構造（ネスト） | AT-15-01〜AT-15-05 |
| R-16 | チェックボックス進捗ウィジェット | AT-16-01〜AT-16-04 |
| R-17 | コピー・カット＆ペースト | AT-17-01〜AT-17-06 |
| R-18 | 複数選択 | AT-18-01〜AT-18-12 |
| R-19 | アクティブエディタへの自動追従 | AT-19-01〜AT-19-07 |
| R-20 | 左右展開レイアウト | AT-20-01〜AT-20-08 |
| R-21 | インライン強調表示（太字・斜体） | AT-21-01〜AT-21-03 |
| NF-01 | パフォーマンス | AT-NF01〜AT-NF02 |
| NF-02 | 信頼性 | AT-NF03〜AT-NF05 |
| NF-03 | セキュリティ | AT-NF06〜AT-NF07 |

---

## R-01 マインドマップ表示

| テストID | 対象要求ID | 前提条件 | 操作 | 期待結果 |
|----------|-----------|---------|------|---------|
| AT-01-01 | R-01-01 | H1〜H6の6段階の見出しを持つMarkdownファイルがVS Codeで開かれている | コマンドパレット `Mind Map: マインドマップとして開く` またはショートカット `Ctrl+Shift+M` でパネルを開く | 6段階すべてが正しく階層化されてノード表示される |
| AT-01-02 | R-01-02 | H1を含むファイルと、H1を含まないファイルがそれぞれVS Codeで開かれている | 各ファイルでマインドマップパネルを開く | H1ありはH1がルートノードになる。H1なしはファイル名がルートノードになる |
| AT-01-03 | R-01-03 | Markdownファイルがエディタで開かれている | エディタタイトルバーのボタンまたは `Ctrl+Shift+M` でマインドマップパネルを開く | Markdownエディタの横（ViewColumn.Beside）にマインドマップパネルが開く |
| AT-01-04 | R-01-05 | マインドマップパネルが起動している | VS Codeのダーク／ライトテーマを切り替える | ノード・接続線・背景の配色がVS Codeテーマに追従する |
| AT-01-05 | R-01-06 | マインドマップパネルが起動しており、ラベルが長く省略（…）表示されているノードが存在する | ラベルが省略されているノードにマウスオーバーする | ツールチップに全文が表示される。省略されていないノードにはツールチップが出ない |
| AT-01-06 | R-01-07 | マインドマップパネルが起動している | パネル上で任意の編集操作（D&D・名称変更・追加等）を実施する | 反映後にVS Codeタブのダーティマーカーが消える（自動保存される） |
| AT-01-07 | R-01-08 | 見出し直後に本文があるノードと、本文がないノードを含むMarkdownでパネルが起動している | 各ノードを確認する | 本文ありにのみドット（●）が表示され、ホバーで本文がツールチップ表示される。本文がないノードにはドットが表示されない |
| AT-01-08 | R-01-09 | ラベルが1行に収まる短いノードと、折り返しが発生する非常に長いラベルのノードを並べて含むMarkdownでパネルが起動している | 各ノードの幅と高さを確認する | ノード幅がラベル長に応じて100〜500px（NODE_MIN_W〜NODE_MAX_W）の範囲で変化する。固定幅（旧260px）にならない。高さはテキストの折り返し有無に応じて1行なら32px（NODE_H_1LINE）、折り返し発生時は46px（NODE_H）に動的決定される。ラベルは最大2行までで省略される |
| AT-01-09 | R-01-04 | 見出しの間に段落・コードブロック等の本文を含むMarkdownでパネルが起動している | ノードの移動（D&D）・削除・追加操作を行う | 操作後も見出し間の本文（段落・コードブロック等）が失われず保持される（自動テスト `markdownRoundTrip.test.ts` のR-01-04コメント付きケースで段落・コードブロック・チェックリストを含む本文のラウンドトリップ保持を検証） |

---

## R-02 ノード移動（ドラッグ＆ドロップ）

| テストID | 対象要求ID | 前提条件 | 操作 | 期待結果 |
|----------|-----------|---------|------|---------|
| AT-02-01 | R-02-01 | マインドマップパネルが起動しており、同じ親ノードに兄弟ノードが2件以上存在する | ノードをドロップ先ノードの上端25%にドロップする。次に下端25%にドロップする | 上端25%で「前に挿入」、下端25%で「後に挿入」となり、Markdownの見出し順が変わる |
| AT-02-02 | R-02-02 | マインドマップパネルが起動しており、2階層以上のノード構造が存在する | ノードをドロップ先ノードの中央50%にドロップする | 「子として追加」となり、レベルが自動更新されMarkdownに反映される |
| AT-02-03 | R-02-03 | マインドマップパネルが起動しており、ノード移動を実施した直後 | Markdownファイルの内容を確認する | ドロップ完了後にMarkdownファイルの内容（見出し順・階層）が更新されている |
| AT-02-04 | R-02-04 | マインドマップパネルが起動しており、ルートノードが表示されている | ルートノードをドラッグする | ドラッグ操作が無視され移動しない |
| AT-02-05 | R-02-05 | マインドマップパネルが起動しており、親子関係のあるノードが表示されている | ノードをその子孫ノードへドロップしようとする | ドロップが拒否され循環参照が発生しない |
| AT-02-06 | R-02-06, R-02-07 | マインドマップパネルが起動しており、複数のノードが表示されている | ノードをドラッグして別の位置へ移動する | ドラッグ中に半透明ゴーストが表示される。before/after はターゲットノードの上下辺に青ライン、inside はターゲットがハイライトされる |
| AT-02-07 | R-02-08 | マインドマップパネルが起動しており、H6ノードが表示されている | H6ノードの中央領域へノードをドラッグ＆ドロップしようとする | `not-allowed` カーソルが表示されドロップが無効化される |
| AT-02-08 | R-02-09 | マインドマップパネルが起動しており、幅の広いノードが表示されている | 幅の広いノードへ、その左端寄り・右端寄りでドラッグ＆ドロップする | ノード矩形の左右いっぱい＋マージン（DROP_TOLERANCE=40）でドロップ判定が効き、中央付近のみに限定されない。縦方向のbefore/after/inside判定は不変 |
| AT-02-09 | R-02-10 | 複数ノードが選択されており、ドロップ先がルートノードのbefore/after等ドロップ先親ノードが解決できない位置である | 選択中のいずれかのノードをドラッグしてドロップ先親ノードが解決できない位置へドロップする | いかなるノードもツリーから除去されずドロップ操作が無視される。ノードが消失しない（自動テスト `dragDrop.test.ts` のR-02-10ケースで`performMultiDrop`の検証ロジックを確認） |

---

## R-03 ノード編集（インライン編集）

| テストID | 対象要求ID | 前提条件 | 操作 | 期待結果 |
|----------|-----------|---------|------|---------|
| AT-03-01 | R-03-01, R-03-02 | マインドマップパネルが起動しており、ノードが表示されている | ノードをダブルクリックしてテキストを変更し `Enter` または別領域クリックで確定する | 入力フィールドが表示され、確定後にMarkdownの見出しテキストが変わる |
| AT-03-02 | R-03-03 | マインドマップパネルでノードのインライン編集中 | `Escape` キーを押す | 変更が破棄され元のテキストに戻る |
| AT-03-03 | R-03-04 | マインドマップパネルでノードのインライン編集中 | 編集内容を空文字にして `Enter` を押す | 変更が適用されない |
| AT-03-04 | R-03-05 | マインドマップパネルが起動しており、ルートノードが表示されている | ルートノードをダブルクリックする。または `F2` を押す。または選択中に `Enter` を押す | いずれの操作でもインライン編集が開始されず何も起きない。子の見出し・本文ノードは従来どおり編集できる |

---

## R-04 ノード追加

| テストID | 対象要求ID | 前提条件 | 操作 | 期待結果 |
|----------|-----------|---------|------|---------|
| AT-04-01 | R-04-01 | マインドマップパネルが起動しており、任意のノードが選択またはフォーカスされている | 右クリック→「子ノードを追加」をクリックする | 親より1段深い見出しがMarkdownに追加される |
| AT-04-02 | R-04-02 | マインドマップパネルが起動しており、ルートノード以外のノードが選択されている | 右クリック→「兄弟ノードを追加」をクリックする | 同レベルの見出しが直後に追加される |
| AT-04-03 | R-04-03, R-04-04 | ノード追加操作（AT-04-01またはAT-04-02）を実施した直後 | 追加されたノードと状態を確認する | デフォルトテキスト「新しいノード」でMarkdownに追加され、直後にインライン編集が自動開始される |

---

## R-05 ノード削除

| テストID | 対象要求ID | 前提条件 | 操作 | 期待結果 |
|----------|-----------|---------|------|---------|
| AT-05-01 | R-05-01, R-05-03 | マインドマップパネルが起動しており、子ノードを持たないノードが選択されている | `Delete` キーを押すか、右クリック→削除を選択する | ノードが削除されMarkdownが更新される |
| AT-05-02 | R-05-02 | マインドマップパネルが起動しており、子ノードを持つノードが選択されている | `Delete` キーを押すか、右クリック→削除を選択する | 確認ダイアログを表示せず、親・子ともに即座にMarkdownから削除される。誤削除した場合は `Ctrl+Z`（Undo）で復元できる |
| AT-05-03 | R-05-04 | マインドマップパネルが起動しており、ルートノードが表示されている | ルートノードを選択して `Delete` キーを押す | 削除操作が無視される |

---

## R-06 折りたたみ

| テストID | 対象要求ID | 前提条件 | 操作 | 期待結果 |
|----------|-----------|---------|------|---------|
| AT-06-01 | R-06-01 | マインドマップパネルが起動しており、子を持つノードが表示されている | 子を持つノードの▼/▶ボタンをクリックする | 子ノードの表示/非表示が切り替わる |
| AT-06-02 | R-06-02, R-06-03 | マインドマップパネルが起動しており、折りたたみ操作を実施した後 | Markdownファイルのフロントマターを確認する | `mindmap-collapse` キーにルートを除いた相対パス（「親/子」形式）が保存される（自動テスト `collapsedPaths.test.ts` / `markdownRoundTrip.test.ts` でパス抽出・適用・相対化・後方互換を検証） |
| AT-06-03 | R-06-04 | 折りたたみ状態のノードを持つMarkdownファイルをVS Codeで保存・閉じた後 | 同じファイルを再度開いてマインドマップパネルを起動する | 折りたたみ状態が復元される。旧形式（ファイル名プレフィックス付き）パスでも復元され、リネーム後も相対パスにより保持される |
| AT-06-04 | R-06-05, R-06-06 | マインドマップパネルが起動しており、ノードが選択されている | ノードを選択しツールバーの「展開」「折畳」ボタンを押す | 折りたたみ可能な状態のときのみ反映される。無効状態（子なし・すでに展開済み・未選択）ではボタンを押しても副作用が発生しない（自動テスト `collapsedPaths.test.ts` で leaf ノード除外・非一致パス無視・同名ノードのフルパス区別を検証） |
| AT-06-05 | R-06-07 | `mindmap-left`により左側ノードを含み、かつルートノードが折りたたまれているマインドマップパネルが起動している | ルートノードの表示、および左右いずれかのブランチの子孫ノードを折りたたむ操作を行う | 左右展開レイアウトでも従来レイアウトと同等に折りたたみが反映される。ルート折りたたみ時は左右どちらの経路でもルートのみが表示されルート配下が描画されない |
| AT-06-06 | R-06-08 | 子見出しを持たず本文リスト項目のみを持つ見出しノードが表示されている | 当該ノードの折りたたみボタン（▼/▶）をクリックして折りたたむ。ファイルを保存・閉じて再度開く | 折りたたみパスがフロントマターに保存され、再オープン時に折りたたみ状態が復元される。判定（子見出しあり または本文項目あり）がWebviewとExtensionで一致し、本文項目のみのノードも保存対象から漏れない |
| AT-06-07 | R-06-09 | 折りたたみ操作直後、かつ外部からの並行変更でコンフリクトが検知され得る状況が用意されている | 折りたたみ状態の保存（`saveCollapseState`）、または本文項目の折りたたみ保存（`saveBodyItemCollapseState`）を発生させる操作を行う | これらの書き込みはコンフリクト検知をスキップして書き込まれ、`baseText`が確実に更新されてから再同期される。誤ってコンフリクトとして破棄されない（自動テスト `mindmapPanel.test.ts` のR-06-09ケースで検証） |
| AT-06-08 | R-06-10 | 管理キー名に類似したユーザー定義キー（例: `my-mindmap-collapse:`）や、`"`・`\`を含む見出しテキストを持つMarkdownファイルが開かれている | 折りたたみ操作を行い保存する。ファイルを再度開く | 管理フロントマターキー（`mindmap-collapse`/`body-item-collapse`/`mindmap-left`）は行頭一致でのみ照合され、類似名のユーザーキーが破壊されない。パス値中の`"`と`\`がエスケープされて書き出され、読み込み時に正しく復元される（自動テスト `collapsedPaths.test.ts` のR-06-10ケースで検証） |

---

## R-07 パン・ズーム

| テストID | 対象要求ID | 前提条件 | 操作 | 期待結果 |
|----------|-----------|---------|------|---------|
| AT-07-01 | R-07-01 | マインドマップパネルが起動しており、ノードと背景が表示されている | 背景または接続線上をドラッグする | ビューがパン（移動）する。ノード上のドラッグはD&Dとなりパンと競合しない |
| AT-07-02 | R-07-02 | マインドマップパネルが起動している | マウスホイールを操作する | カーソル位置を中心にズームする |
| AT-07-03 | R-07-03, R-07-04 | マインドマップパネルが起動しており、ビューが移動・拡縮されている | ツールバーの「フィット」ボタンをクリックするか `F` キーを押す | ツリー全体が画面内に収まる倍率・位置に自動調整される |
| AT-07-04 | R-07-04 | マインドマップパネルが起動している | `Ctrl++` / `Ctrl+-` を押す | ズームイン／アウトが動作する |
| AT-07-05 | R-07-05 | マインドマップパネルが起動しており、ルートノードと子ノードが表示されている。VS Code のライト／ダーク／高コントラスト各テーマへ切り替えられる | 各テーマでルートノードの非選択時と選択時の見た目を確認する | 非選択時はルートノードに他ノードの階層アクセント色とは明確に異なる紫系の外周枠が表示され、左3pxバーは表示されない。枠は各テーマの背景に埋没しない。選択時は通常の紫系枠を残したまま、間隔付きのフォーカス色外側リングが追加され、非選択時と区別できる。他ノードは従来どおり階層色のアクセントバー表示を維持する |

---

## R-08 ノード上下入れ替え

| テストID | 対象要求ID | 前提条件 | 操作 | 期待結果 |
|----------|-----------|---------|------|---------|
| AT-08-01 | R-08-01, R-08-04 | マインドマップパネルが起動しており、同じ親ノードに兄弟ノードが2件以上存在し、いずれかが選択されている | 右クリック→「↑ 上へ移動」または「↓ 下へ移動」をクリックする | 選択ノードが1つ上/下の兄弟と入れ替わりMarkdownの見出し順に反映される |
| AT-08-02 | R-08-02 | マインドマップパネルが起動しており、ノードが選択されている | `Alt+↑` / `Alt+↓` を押す | 選択ノードが上下に移動する |
| AT-08-03 | R-08-03 | マインドマップパネルが起動しており、先頭または末尾の兄弟ノードが選択されている | 右クリックメニューを開いて「↑ 上へ移動」（先頭の場合）または「↓ 下へ移動」（末尾の場合）を確認する | 先頭ノードの「上へ」・末尾ノードの「下へ」がグレーアウトされ操作不可になっている |

---

## R-09 ファイル保存（Ctrl+S）

| テストID | 対象要求ID | 前提条件 | 操作 | 期待結果 |
|----------|-----------|---------|------|---------|
| AT-09-01 | R-09-01, R-09-02 | マインドマップパネルが起動しており、フォーカスがビュー上にある | `Ctrl+S`（Mac: `Cmd+S`）を押す | 直前のマインドマップ操作を含めてMarkdownが保存され、ダーティマーカーが消える |
| AT-09-02 | R-09-03 | マインドマップパネルが起動しており、保存（自動または手動）が行われた直後 | ツールバーを確認する | 「✓ 保存済」インジケーターが約1.8秒フェード表示され、連続保存でタイマーがリセットされる |

---

## R-10 Undo

| テストID | 対象要求ID | 前提条件 | 操作 | 期待結果 |
|----------|-----------|---------|------|---------|
| AT-10-01 | R-10-02, R-10-03 | マインドマップパネルで複数の編集操作を実施した後 | `Ctrl+Z` を1回押す | 1操作分だけ戻り、Markdownファイルがスナップショット時点の状態になる |
| AT-10-02 | R-10-01 | マインドマップパネルで51回以上の操作を実施した後 | `Ctrl+Z` を繰り返す | 最古のエントリーが破棄され、保持される履歴は最大50件 |
| AT-10-03 | R-10-05 | マインドマップパネルでUndoを実施した直後 | 選択状態を確認する | 選択ノードが存在しない状態になる |
| AT-10-04 | R-10-04 | マインドマップパネルでノードのD&D等の操作を実施した直後 | Undoスタックに積まれたスナップショットの内容を確認する | スナップショットにノードのレイアウト座標（`_x`,`_y`,`_sh`）が含まれず、`id`/`text`/`level`/`collapsed`/`body`/`side`/`collapsedBodyLines`/`children`の論理データのみが保存されている（自動テスト `webviewSyncGuard.test.ts` のR-10-04ケースで`cloneForUndo`を検証） |

---

## R-11 双方向同期の競合管理

| テストID | 対象要求ID | 前提条件 | 操作 | 期待結果 |
|----------|-----------|---------|------|---------|
| AT-11-01 | R-11-01 | マインドマップパネルでD&Dやインライン編集の確定処理が実行中 | 別タブや別エディタでMarkdownを直接編集する | 操作中はMarkdown変更イベントが無視され、パネルが中断されない |
| AT-11-02 | R-11-02, R-11-03 | マインドマップのD&D等の操作が完了した後 | パネルの状態を確認する | Markdownをソースオブトゥルースとして再パース・再描画され、競合時はMarkdown側の内容が最終反映される |
| AT-11-03 | R-11-04, R-11-05 | マインドマップパネルを開いた後、外部（別タブ・共有ドライブ上の他者）で同じ.mdを変更した状態 | マインドマップで構造編集操作を実施する | 書き込み直前にbase不一致が検知され、黙って上書きされずモーダル警告が表示される（自動テスト `conflictDetection.test.ts` で判定ロジックを検証） |
| AT-11-04 | R-11-06, R-11-07 | コンフリクト警告モーダルが表示されている | 「最新を読み込む（自分の編集は破棄）」を選択する | 自分の書き込みが中止され、ディスクの最新内容で再同期される。自分の版が `*.conflict-mine-*.md` として退避される |
| AT-11-05 | R-11-06, R-11-07 | コンフリクト警告モーダルが表示されている | 「自分の変更で上書き（他者の変更は破棄）」を選択する | マインドマップの編集で全文置換・保存される。他者の版が `*.conflict-remote-*.md` として退避される |
| AT-11-06 | R-11-05 | 改行コードのみが異なる（CRLF↔LF）ファイルでマインドマップパネルが起動している | 構造編集操作を実施する | コンフリクトとは扱われず、警告なしで従来どおり保存される（自動テスト `conflictDetection.test.ts` で CRLF/CR/LF 混在の非衝突・末尾改行差の衝突・空 base 判定を検証） |
| AT-11-07 | R-11-08 | マインドマップパネルで操作中（D&D確定等）に外部変更を割り込ませた後、操作が完了する | 操作完了後の書き込みを確認する | 操作中はイベント無視（中断なし）だが、完了後の書き込み前検知でコンフリクトとして扱われ警告が表示される |
| AT-11-08 | R-11-09 | マインドマップパネルでインライン編集中またはドラッグ中に、外部変更を含むtree updateを受信する状況を用意する | 操作中にupdateを受信させ、操作終了後の状態を確認する。あわせて、外部変更後の世代より古い世代からのstructuralEditを発生させる | 操作中に受信したupdateは破棄されず最新1件のみ保留され、操作終了後に未確定の構造編集がなければ適用される。操作中はrootの即時差し替えを行わない。最新世代より古い世代からのstructuralEditはR-11-06のコンフリクト解決フローに乗る（送信内容がライブ内容と一致するエコーは除く）（自動テスト `syncGuard.test.ts`／`webviewSyncGuard.test.ts` のR-11-09ケースで検証） |

---

## R-12 キーボードナビゲーション

| テストID | 対象要求ID | 前提条件 | 操作 | 期待結果 |
|----------|-----------|---------|------|---------|
| AT-12-01 | R-12-01 | マインドマップパネルが起動しており、同一親に複数の見出し兄弟ノードが存在し、そのうち1つが選択されている | `↑` / `↓` キーを押す | 同一親の可視兄弟見出しノード間だけで前後に移動する。子ノード・親ノード・本文項目へは移動しない。ルート選択中の `↑` / `↓` は何も起きない |
| AT-12-02 | R-12-02 | マインドマップパネルが起動しており、右/左ブランチの見出しノード、またはルートノードが選択されている | 右ブランチで `→`、左ブランチで `←`、ルートで `→` を押す | ブランチ上では画面上の子方向へ移動する。最初の子見出しを優先し、子見出しがなければ最初の本文ノードへ移動する。折りたたみ中なら展開して同じ操作内で先頭子へ移動し、子なしなら何もしない。ルートの `→` は右側の先頭可視直下ノードへ移動し、右ノードがなければ何もしない |
| AT-12-03 | R-12-03 | マインドマップパネルが起動しており、右/左ブランチの見出しノード、またはルートノードが選択されている | 右ブランチで `←`、左ブランチで `→` を押す。展開中ノードでは同じキーをもう一度押す。ルートでは `←` を押す | ブランチ上では画面上の親方向へ動作する。子見出しまたは本文ノードを持つ展開中ノードは一度目で折りたたまれ選択を維持し、次回で親へ移動する。折りたたみ済み/子なしなら一度で親へ移動する。ルートの `←` は左側の先頭可視直下ノードへ移動し、左ノードがなければ何もしない |
| AT-12-04 | R-12-04 | マインドマップパネルが起動しており、ルートノード以外のノードが選択されている | `Enter` キーを押す | 直後に兄弟ノードを追加しインライン編集を開始する |
| AT-12-05 | R-12-04b | マインドマップパネルが起動しており、ノードが選択されている | `F2` キーを押す | 選択ノードのインライン編集が開始される |
| AT-12-06 | R-12-05 | マインドマップパネルが起動しており、H6以外のノードが選択されている | `Tab` キーを押す | 子ノードを追加し編集開始する。H6ノードでは無効化される |
| AT-12-07 | R-12-06 | マインドマップパネルが起動しており、ノードが選択されており、インライン編集中ではない | `Escape` キーを押す | 選択ハイライトが消える |
| AT-12-08 | R-12-08 | マインドマップパネルが起動しており、画面外にノードが存在する | キーボードで画面外のノードへ選択を移動する | ビューが自動スクロールして対象ノードが表示される（ノードが60px以上画面端に近い場合に自動パン） |
| AT-12-09 | R-12-09 | マインドマップパネルでインライン編集中（`editingId` が設定中） | ナビゲーション系キー（↑↓→←等）を押す | ナビゲーションが無効化され、入力フィールド内の操作と競合しない |
| AT-12-10 | R-12-11 | マインドマップパネルが起動しており、右/左ブランチ上の本文項目が選択され、同じ直接親の兄弟本文項目または子本文項目が存在する | `↑` / `↓`、右ブランチで `→` / `←`、左ブランチで `←` / `→` を押す。展開中ノードの親方向キーは同じキーをもう一度押す | `↑` / `↓` は同じ直接親を持つ可視兄弟本文項目間だけを移動し、階層・見出し境界を跨がない。子方向キーは折りたたみ中なら展開して同じ操作内で先頭子へ移動する。親方向キーは展開中なら一度目で折りたたみ、次回に `indent=0` は親見出し、`indent>0` は親本文ノードへ移動する |
| AT-12-11 | R-12-11 | 本文項目が選択されており、Markdown再パースにより見出しIDまたは本文行番号が再採番される。あわせて、一意に復元できるケースと同一祖先・同一内容が重複して曖昧になるケースを用意する | 選択中にツリー `update` を発生させる | 一意ケースでは更新前の見出し祖先パスと本文項目祖先パスに一致する現行本文項目へ選択が再束縛される。再採番ID/行番号だけでは照合しない。削除済みまたは複数候補で一意に復元できないケースでは本文選択が解除され、別項目へ誤選択されない |
| AT-12-12 | R-12-07 | マインドマップパネルが起動しており、子ノードを持たないノード・子ノードを持つノードがそれぞれ選択できる | 各ノードを選択し `Delete` キーを押す | いずれの場合も確認ダイアログを表示せず即座に削除される（R-05-01と統合された動作）。誤削除した場合は`Ctrl+Z`で復元できる |
| AT-12-13 | R-12-10 | Undoスタックが空の状態でマインドマップパネルが起動している | `Ctrl+Z` を押す | Undoスタックが空の場合は何も起こらない（エラーが発生せず、ツリー状態が変化しない） |

---

## R-13 本文項目のマインドマップツリー表示

| テストID | 対象要求ID | 前提条件 | 操作 | 期待結果 |
|----------|-----------|---------|------|---------|
| AT-13-01 | R-13-01, R-13-02 | `- [ ] item` / `- [x] item` / `- item` を含む本文を持つ見出しのMarkdownでパネルが起動している | パネルの各本文ノードを確認する | 本文項目が見出しの右に破線・半透明・小フォントの本文ノードとして表示され、接続線が破線グレーになる |
| AT-13-02 | R-13-03 | マインドマップパネルが起動しており、チェックボックス本文ノードが表示されている | チェックボックス本文ノードのチェックボックスをクリックする | チェック状態が即時トグルされMarkdownに保存される。`- [x]` 行はラベルに取り消し線・ノードが半透明になる |
| AT-13-03 | R-13-04 | マインドマップパネルが起動しており、`- item`（チェックボックスなし）の本文ノードが表示されている | 本文ノードの表示を確認する | ダッシュ（–）付きで表示され、チェックボックス行と区別できる |
| AT-13-04 | R-13-05 | マインドマップパネルが起動しており、本文ノードが表示されている | 本文ノードをダブルクリックまたは `F2` でインライン編集し、テキストを変更して確定する | テキストのみ編集でき、`- [ ] ` プレフィックスが自動付与される。確定後にMarkdownの該当行が更新される |
| AT-13-05 | R-13-06 | マインドマップパネルが起動しており、本文ノードが選択されている | `Delete` キーを押す | 本文行が削除されMarkdownが更新される |
| AT-13-06 | R-13-07 | マインドマップパネルが起動しており、本文を持つ見出しノードが表示されている | 本文を持つ見出しの折りたたみボタン（▼/▶）をクリックする | 本文ノードも折りたたみ対象として非表示になる |
| AT-13-07 | R-13-09 | マインドマップパネルが起動しており、本文ノードが選択されている | `Enter` キーを押すか、右クリック→「同階層に追加」をクリックする | 同一親・同一インデントの兄弟本文項目がサブツリー末尾の直後に追加され、自動編集に入る |
| AT-13-08 | R-13-10, R-02-09 | マインドマップパネルが起動しており、本文ノードが表示されている | トップレベル本文ノードをドラッグしてネスト位置（indent>0）へドロップする | 子項目ごと移動するが、チェックボックス/箇条書きの種別は移動先インデントに関わらず変化しない（自動変換は行われない）。ドロップ位置は水平ラインで示される（自動テスト `bodyItems.test.ts` の `reformatBodyLines` で複数レベルのインデント変換時も種別が維持されることを検証） |
| AT-13-09 | R-01-09 | 長文・短文の本文項目を並べて含むMarkdownでパネルが起動している | 各本文ノードの幅と高さを確認する | 本文ノード幅が80〜450px（BODY_MIN_W〜BODY_MAX_W）の範囲で動的に変化し、高さは1行なら30px（BODY_H_1LINE）、折り返し発生時は42px（BODY_H）に動的決定され、ラベルは最大2行まで表示される |
| AT-13-10 | R-13-10, R-02-09 | マインドマップパネルが起動しており、幅の広い本文ノードが表示されている | 幅の広い本文ノードへ、その左端寄り・右端寄りでドラッグ＆ドロップする | 中央付近だけでなく本文ノード矩形の左右いっぱい＋マージンでドロップ判定が効き、水平ラインまたはinsideハイライトが表示される |
| AT-13-11 | R-13-11 | トップレベルにプレーン箇条書き（`- text`）とチェック済み（`- [x]`）項目を含む.mdファイルがVS Codeで開かれている | マインドマップビューをコマンドで開く | 開いた直後もプレーン箇条書きはチェックボックスへ自動変換されず、`- [x]` のチェック状態もそのまま維持される。ファイルへの書き込み・保存は発生せずダーティマーカーが付かない（`normalizeBodyCheckboxes`/`normalizeTreeCheckboxes` は `src/bodyItems.ts` に存在せず、自動変換ロジック自体が撤廃されていることをコードで確認） |
| AT-13-12 | R-13-11 | マインドマップパネルが起動しており、チェックボックス項目・ダッシュ項目を含む本文ノードが表示されている | 既存項目をインライン編集で確定する。別インデント位置へドラッグ＆ドロップで移動する。新規本文項目を追加する | いずれの操作でも既存項目の種別（チェックボックス/箇条書き）は自動変換されない。新規追加した本文項目は常に箇条書き（`- text`）として作成される（自動テスト `bodyItems.test.ts` の `reformatBodyLines` 系ケースで移動時の種別維持を検証） |
| AT-13-13 | R-13-12 | コードフェンス（```/~~~、インデント・言語ラベルあり）内にリスト風の行（`- [ ] fake` 等）を含む本文を持つ見出しのMarkdownでパネルが起動している | 該当見出しのツリー表示と進捗ウィジェットを確認する | フェンス内のリスト風行は本文ノードとしてツリー表示されず、進捗ウィジェットのチェックボックス総数・完了数のカウントにも含まれない（自動テスト `bodyItems.test.ts` のR-13-12ケースで検証） |
| AT-13-14 | R-13-13 | ルートノードが表示されている | ルートノードを右クリックしてコンテキストメニューを確認し、「本文項目を追加」を選択しようとする | 「本文項目を追加」メニュー項目が無効化（グレーアウト）されており、選択しても本文項目が追加されない |
| AT-13-15 | R-13-14 | チェックボックスフィルタが「✓ ONのみ」に設定されている状態、および対象本文項目の親が折りたたまれている状態でそれぞれパネルが起動している | フィルタ「✓ ONのみ」の状態でトップレベル本文項目を新規追加する。次に、親が折りたたまれた状態で子本文項目を新規追加する | トップレベル追加時はフィルタが自動的に「すべて」へ戻り追加項目が表示され編集が開始される。子項目追加時は親項目の折りたたみが解除されて追加項目が表示され編集が開始される。いずれも保留updateの適用や外部同期が停止しない |
| AT-13-16 | R-13-15 | チェック済み・未チェックのチェックボックス項目とダッシュ項目、ネスト本文項目を含むパネルが起動している | ツールバーの「すべて」「✓ ONのみ」「□ OFFのみ」ボタンを順にクリックする | 「✓ ONのみ」選択時はチェック済みのトップレベルチェックボックス項目のみ表示され、「□ OFFのみ」選択時は未チェックのトップレベルチェックボックス項目のみ表示される。ダッシュ項目・ネスト（indent>0）項目はフィルタに関わらず常に表示される。フィルタ変更でMarkdownファイルの内容・保存挙動は変化しない。パネルを開き直すとフィルタは「すべて」に戻る |
| AT-13-17 | R-13-16 | チェックボックス項目とダッシュ項目（トップレベル・ネストとも）が表示されている | チェックボックス項目を右クリックして「チェックボックスをやめる」を選択する。ダッシュ項目を右クリックして「チェックボックスにする」を選択する | チェックボックス→ダッシュ、ダッシュ→チェックボックスへ種別が切り替わる。ダッシュからチェックボックスへ変換した場合は常に未チェック（`- [ ] `）状態で開始される。トップレベル・ネストいずれでも切替が可能（自動テスト `bodyItems.test.ts` の `toggleBodyItemType` 関連ケースで検証） |
| AT-13-18 | R-13-08 | 見出し直後に非リスト形式の本文（段落・コードブロック等）のみを持つノードが表示されている | 該当見出しノードのツリー表示とドットインジケーターを確認する | 非リスト本文行はツリー上に本文ノードとして表示されず、見出しノード上のドット（●）インジケーターのホバーで本文全文がツールチップ表示される |

---

## R-14 本文項目⇔見出しノードの変換

| テストID | 対象要求ID | 前提条件 | 操作 | 期待結果 |
|----------|-----------|---------|------|---------|
| AT-14-01 | R-14-01 | マインドマップパネルが起動しており、トップレベル本文項目、ネスト（indent>0）本文項目、および親見出しがH6である本文項目が存在する | 各本文項目を右クリックしコンテキストメニューを確認する | トップレベル本文項目のみ「見出しにする」が有効。ネスト本文項目、および親がH6の本文項目では無効化（グレーアウト）される |
| AT-14-02 | R-14-02 | 子本文項目を持つトップレベル本文項目が選択されている | 右クリック→「見出しにする」を選択する | 選択項目のテキストが親レベル+1の新規見出しノードとなり、親ノードの既存子見出しより前（`children`の先頭）に挿入される。ネストしていた子本文項目はインデントを0起点に振り直され、新規見出しノードの本文として保持される |
| AT-14-03 | R-14-03 | チェック済み（`- [x]`）のトップレベル本文項目が選択されている | 右クリック→「見出しにする」を選択する | 新規見出しノードが作成され、チェック状態の情報は破棄される（見出しノードにチェック概念がないこと） |
| AT-14-04 | R-14-04 | 子見出しを持たない見出しノード、子見出しを持つ見出しノード、ルートノードがそれぞれ存在する | 各ノードを右クリックしコンテキストメニューを確認する | 子見出しを持たない見出しノードのみ「本文項目にする」が有効。子見出しを持つノード・ルートノードでは無効化される |
| AT-14-05 | R-14-05 | 子見出しを持たず本文も持つ見出しノードが選択されている | 右クリック→「本文項目にする」を選択する | 対象ノードのテキストが箇条書き（`- text`、チェックボックスなし）として親ノードの本文末尾に追加され、対象ノード自身の本文はその子項目としてインデント+2で続けて追加される。対象ノードはツリーから削除される |
| AT-14-06 | R-14-06 | 昇格操作または降格操作を実施した直後 | `Ctrl+Z` を押す | 昇格・降格前の状態（本文項目/見出しノードの構成）へ1回のUndoで復元される |

---

## R-15 本文項目の階層構造（ネスト）

| テストID | 対象要求ID | 前提条件 | 操作 | 期待結果 |
|----------|-----------|---------|------|---------|
| AT-15-01 | R-15-01 | 親項目より2スペース多いインデントの本文行を持つMarkdownでパネルが起動している | ツリー上の子本文ノードの位置を確認する | インデント行が子本文項目としてツリー表示され、親の実幅＋ギャップ（BODY_ITEM_GAP=12px）位置に配置されて重ならない |
| AT-15-02 | R-15-02 | マインドマップパネルが起動しており、本文ノードが表示されている | 子階層の本文項目を新規追加する | チェックボックスを持たず `- text`（ダッシュ）形式で保存・表示される |
| AT-15-03 | R-15-03, R-15-04 | マインドマップパネルが起動しており、本文ノードが選択されている | `Tab` キーを押すか、右クリック→「子項目を追加」をクリックする | 親より2スペース多いインデントで子項目が追加され、直後に編集開始される |
| AT-15-04 | R-15-05 | マインドマップパネルで子項目を持つ本文項目を折りたたみ、ファイルを保存・閉じた後 | 同じファイルを再度開いてパネルを起動する | 子項目が非表示になり、折りたたみ状態がフロントマター `body-item-collapse:` に保存され再オープン時も復元される（自動テスト `markdownRoundTrip.test.ts` で `body-item-collapse` ブロックの保全・複数パスの順序保全・title/mindmap-collapse との3ブロック共存を検証） |
| AT-15-05 | R-15-06 | マインドマップパネルが起動しており、本文ノードが子本文ノードを持っている | 本文ノード→子本文ノードの接続線を確認する | 見出し→本文項目と同じ破線グレースタイルで表示される（見出し間の実線と視覚的に区別できる） |

---

## R-16 チェックボックス進捗ウィジェット

| テストID | 対象要求ID | 前提条件 | 操作 | 期待結果 |
|----------|-----------|---------|------|---------|
| AT-16-01 | R-16-01 | チェックボックスを含むMarkdownと含まないMarkdownをそれぞれ開いたパネルが起動している | ツールバーの進捗ウィジェット表示を確認する。次にチェック状態を変化させる | チェックボックスがある場合のみ「✓ 完了数 / 総数」が表示され、ない場合は非表示。状態変更で即時更新される。最大9999件まで見切れない最小幅（92px）が確保される |
| AT-16-02 | R-16-02 | マインドマップパネルが起動しており、チェックボックスが複数存在する | チェック状態を変化させる | 背景の半透明グリーン進捗バーが進捗率に応じて幅変化しアニメーションする |
| AT-16-03 | R-16-03 | マインドマップパネルが起動しており、チェックボックスがすべて完了状態になっている | 全チェックボックスを完了させる。次に一部を未完了に戻す | 全完了で進捗ウィジェットの枠線がグリーンになる。未完了に戻ると枠線色が戻る |
| AT-16-04 | R-16-04 | マインドマップパネルが起動しており、ツールバーが表示されている | ツールバーの「?」ボタンをクリックする | ショートカット一覧ポップアップがツールバー右端に表示される。ボタン再クリック・領域外クリックで閉じる。見出しノード・本文項目・共通の3セクションに分類表示される |

---

## R-17 コピー・カット＆ペースト

| テストID | 対象要求ID | 前提条件 | 操作 | 期待結果 |
|----------|-----------|---------|------|---------|
| AT-17-01 | R-17-01, R-17-02 | マインドマップパネルが起動しており、見出しノードが選択されている | `Ctrl+C` でコピー後、別ノードを選択して `Ctrl+V` で貼り付ける | 子・本文を含む全ツリーがコピーされ、選択ノードの子階層末尾にレベル調整・新ID付番で貼り付けられる。選択ノードがH6なら貼り付けない |
| AT-17-02 | R-17-03, R-17-04 | マインドマップパネルが起動しており、本文ノードが選択されている | `Ctrl+C` でコピー後、別の本文ノードを選択して `Ctrl+V` で貼り付ける | 子項目を含めコピーされ、選択本文項目のサブツリー末尾直後に階層に応じた形式（チェックボックス/ダッシュ）で貼り付けられる |
| AT-17-03 | R-17-05 | マインドマップパネルで本文項目をクリップボードにコピーしており、見出しノードが選択されている | `Ctrl+V` で貼り付ける | トップレベル本文（`- [ ] text`）として見出しの本文末尾に追加される |
| AT-17-04 | R-17-07 | マインドマップパネルが起動しており、見出しノードが選択されている | `Ctrl+X` でカットする | コピー後に元ノードがツリーから削除される |
| AT-17-05 | R-17-08 | マインドマップパネルが起動しており、本文ノードが選択されている | `Ctrl+X` でカットする | コピー後に元の本文行がMarkdownから削除される |
| AT-17-06 | R-17-06 | マインドマップパネルでインライン編集中（`editingId` が設定中） | `Ctrl+C` / `Ctrl+X` / `Ctrl+V` を押す | マインドマップ操作が無効化され、入力フィールド内のテキストコピーペーストが正常動作する |

---

## R-18 複数選択

| テストID | 対象要求ID | 前提条件 | 操作 | 期待結果 |
|----------|-----------|---------|------|---------|
| AT-18-01 | R-18-01 | マインドマップパネルが起動しており、同じ親を持つ兄弟見出しノードが複数、かつ異なる親を持つ見出しノードも存在する | `Ctrl+クリック`（Mac: `Cmd+クリック`）で同じ親を持つ兄弟ノードを複数選択し、選択済みノードを再クリックする。次に、選択中とは異なる親を持つノードを `Ctrl+クリック` する | 同一親の兄弟ノードは複数選択状態になる。選択済みノードの再クリックで解除される。異なる親のノードを `Ctrl+クリック` すると既存の複数選択は解除され、そのノード単体の選択に置き換わる（異なる階層をまたぐ複数選択の拡張はできない） |
| AT-18-02 | R-18-02 | マインドマップパネルが起動しており、複数の本文ノードが表示されている | `Ctrl+クリック` で複数の本文ノードを選択し、選択済みノードを再クリックする | 複数本文ノードが選択でき、再クリックで解除される |
| AT-18-03 | R-18-03 | マインドマップパネルが起動しており、複数ノードが選択されている | 選択中のいずれかのノードをドラッグして別の場所へドロップする | 複数ノードがまとめて移動する。移動先の子孫である選択ノードはドロップ対象から除外される |
| AT-18-04 | R-18-04 | マインドマップパネルが起動しており、複数ノードが選択されている | `Ctrl+C` でコピー後、別ノードを選択して `Ctrl+V` で貼り付ける | 選択中の全ノードがペースト先の子階層にまとめて追加される |
| AT-18-05 | R-18-05 | マインドマップパネルが起動しており、複数ノードが選択されている | `Ctrl+X` でカットする | 選択中の全ノードがコピーされ、元ノードがすべて削除される |
| AT-18-06 | R-18-06 | マインドマップパネルが起動しており、複数ノードが選択されている | `Delete` キーを押す | 選択中の全ノードが確認ダイアログなしで即座に削除される。子を持つノードが含まれる場合も確認ダイアログは表示されない。誤削除した場合は `Ctrl+Z`（Undo）で復元できる |
| AT-18-07 | R-18-07 | マインドマップパネルが起動しており、複数ノードが選択されている | 右クリックまたは矢印キーまたは `F2` を操作する | 複数選択が解除され単一選択として動作する |
| AT-18-08 | R-18-08 | マインドマップパネルが起動しており、複数ノードが選択されている | `Escape` キーを押す | 複数選択がすべてクリアされる |
| AT-18-09 | R-18-09 | 同一親を持つ子見出しを持たない見出しノードが2件以上複数選択されている。別途、選択中のいずれかが子見出しを持つケースも用意する | 右クリック→「本文項目にする」を選択する。子見出しを持つケースでは同メニューの状態を確認する | 子見出しを持たないケースでは選択中の全ノードが一括で本文項目へ降格される。子見出しを持つノードが選択に含まれる場合はメニューが無効化される。Undoは1操作として記録され、`Ctrl+Z`で全ノードが一括復元される |
| AT-18-10 | R-18-10 | 同一親・同一インデントのトップレベル本文項目が2件以上複数選択されている | 右クリック→「見出しにする」を選択する | 選択中の全本文項目が一括で見出しノードへ昇格される。Undoは1操作として記録され、`Ctrl+Z`で全項目が一括復元される |
| AT-18-11 | R-18-11 | チェックボックス項目とダッシュ項目が混在する複数の本文項目が同一親・同一インデントで複数選択されている | 右クリック対象項目（チェックボックス項目）を右クリックし「チェックボックスをやめる」を選択する | 右クリック対象の現在種別を基準に選択中の全項目が同一種別（ダッシュ）へ一括変換される。Undoは1操作として記録され、`Ctrl+Z`で全項目が一括復元される |
| AT-18-12 | R-18-12 | 複数の本文項目が選択されている | `Delete` キーを押す | 選択中の全本文項目が確認ダイアログなしで一括削除されMarkdownが更新される（R-18-06の本文項目版） |

---

## R-19 アクティブエディタへの自動追従

| テストID | 対象要求ID | 前提条件 | 操作 | 期待結果 |
|----------|-----------|---------|------|---------|
| AT-19-01 | R-19-01 | マインドマップパネルが起動している状態で、別の.mdファイルがVS Codeのタブに存在する | 別の.mdファイルのタブをクリックしてアクティブにする | ビューアの表示内容・タイトルが新しいファイルに自動追従する |
| AT-19-02 | R-19-02 | マインドマップパネルが起動している状態 | VS Codeの設定画面・出力パネル・画像などの非Markdownドキュメントにフォーカスする | ビューアは直前のMarkdown内容を維持し、クリア・破棄されない |
| AT-19-03 | R-19-03 | マインドマップパネルが起動しており、対象ドキュメントが開かれている | 同一.mdを対象にマインドマップコマンドを複数回実行する | 新規パネルが重複生成されず、既存パネルがアクティブになる |
| AT-19-04 | R-19-04 | マインドマップパネルで編集操作中（`_editQueue` に書き込みが残っている）の状態 | 別の.mdファイルへアクティブエディタを切り替える | 旧ドキュメントへの書き込みが完了してから切り替わり、新ドキュメントで矛盾なく編集できる |
| AT-19-05 | R-19-05 | VS Codeの設定で `mindmap.followActiveEditor` を `false` に設定した状態でパネルが起動している | 別の.mdファイルへアクティブエディタを切り替える | ビューアが追従せず、以前のファイルを表示し続ける |
| AT-19-06 | R-19-06 | マインドマップパネルが起動している | VS Code設定 `mindmapEditor.fontSize` の値を変更する（既定14px、範囲8〜32） | 拡張機能の再起動なしで設定変更が即座にWebviewへ反映され、ノードのフォントサイズが変更される |
| AT-19-07 | R-19-07 | マインドマップパネルが起動している | VS Code設定 `mindmap.edgeWidth` の値を変更する（既定1.5px、範囲0.5〜8） | 拡張機能の再起動なしで設定変更が即座にWebviewへ反映され、ノード間接続線の太さが変更される（自動テスト `edgeWidth.test.ts` のR-19-07ケースで`cfgSub`のedgeWidth設定変更ハンドリングと`ready`時の初期edgeWidth送信を検証） |

---

## R-20 左右展開レイアウト

| テストID | 対象要求ID | 前提条件 | 操作 | 期待結果 |
|----------|-----------|---------|------|---------|
| AT-20-01 | S20-01, S20-02 | `mindmap-left`フロントマターにH1ノードのテキストが1件記載されたファイル、および`mindmap-left`が存在しないファイルをそれぞれ用意する | 各ファイルでマインドマップパネルを開く | `mindmap-left`に記載されたテキストと一致するルート直下ノードに`side:'left'`が付与され左側に配置される。それ以外のルート直下ノードは`side:'right'`（デフォルト）になる。`mindmap-left`がないファイルはすべてのルート直下ノードが`side:'right'`になる（自動テスト `markdownRoundTrip.test.ts` のS20-01/S20-02ケースで検証） |
| AT-20-02 | S20-03, S20-04, S20-06 | 左側ノード（`side:'left'`）を含むマインドマップパネルが起動している | レイアウト上の各ノードの座標を確認する | ルートノードはキャンバス左端から左側サブツリー幅分オフセットして配置される。左側H1ノードとその配下（H2以下）は親の左端-`H_GAP`-子幅のx座標に配置される。全ノードの最小x座標が`PAD`未満にならないよう右シフトされる。左側ノードが存在しない場合はルートが`x=PAD`に配置される |
| AT-20-03 | S20-05 | 左側ノードと右側ノードの両方を含むマインドマップパネルが起動している | 各ノードの接続線を確認する | 左側ノード配下の接続線は親の左端から子の右端へ引かれる。右側ノード配下の接続線は従来どおり親の右端から子の左端へ引かれる |
| AT-20-04 | S20-07, S20-08, S20-09 | ルートノードとH1子ノードが表示されている | H1ノードをドラッグしてルートノードの左半分・右半分それぞれへドロップする。ドラッグ中にルート上のドロップ位置表示を確認する | ドラッグ中はルートの左ゾーン・右ゾーンがそれぞれ`drop-root-left`/`drop-root-right`クラスでハイライトされる。左半分でドロップすると`side:'left'`、右半分でドロップすると`side:'right'`に設定され、それぞれのレイアウトで再配置される |
| AT-20-05 | S20-10 | H2以下の子孫ノードが表示されている | H2以下のノードをドラッグしてルートノードへドロップする | ドロップされたノードはH1（level=1）に昇格された上でsideが設定される |
| AT-20-06 | S20-11, S20-12 | `side='left'`のルート直下ノードを持つマインドマップパネルが起動している | side変更操作（D&Dでのside付与・解除）を行いファイルを保存する。ファイルを再度開く | `side:'left'`のノードのテキストが相対パス形式でフロントマター`mindmap-left`キーに保存され、再オープン時に復元される。左側ノードが存在しなくなった場合は`mindmap-left`ブロックがフロントマターから削除される（自動テスト `markdownRoundTrip.test.ts` のS20-03〜S20-06ケースで検証） |
| AT-20-07 | S20-13 | マインドマップパネルが起動しており、H1ノードが選択されている | side変更操作（ドラッグ＆ドロップ）を実施する | Webview→Extensionへ`setSide`メッセージが送信され、対象ルート直下ノードのsideが更新されフロントマターへ反映・保存される |
| AT-20-08 | S20-14 | 左側ノードを含み、かつルートノードが折りたたまれているマインドマップパネルが起動している | ルートノードの表示状態を確認する | 左右レイアウト経路でも`root.collapsed`が尊重され、ルートのみが配置されルート配下（左側・右側ノード）は描画されない |

---

## R-21 インライン強調表示（太字・斜体）

| テストID | 対象要求ID | 前提条件 | 操作 | 期待結果 |
|----------|-----------|---------|------|---------|
| AT-21-01 | R-21-01 | `**bold**`、`*italic*`、`***both***`、および閉じられていない`*`を含む見出し・本文項目のラベルを持つMarkdownでパネルが起動している | 各ノード・本文項目のラベル表示を確認する | `**text**`/`__text__`は太字、`*text*`/`_text_`は斜体、`***text***`/`___text___`は太字+斜体として装飾表示される。閉じられていない`*`/`_`はリテラル表示される（自動テスト `inlineMarkdown.test.ts` のR-21-01ケースで検証） |
| AT-21-02 | R-21-02 | ラベルに `<script>alert(1)</script>` や `<img src=x onerror=...>` などHTMLタグを含むノード・本文項目を持つMarkdownでパネルが起動している | 該当ノードのラベル表示とDevToolsコンソールを確認する | `&`,`<`,`>`,`"`,`'`がエスケープされてプレーンテキストとして表示され、タグが実行・注入されない（自動テスト `inlineMarkdown.test.ts` のR-21-02ケースで検証） |
| AT-21-03 | R-21-03 | 太字・斜体記法を含むノード・本文項目が表示されている | ダブルクリックまたは`F2`でインライン編集を開始する | 編集用入力欄には装飾が適用されず、`**text**`等の生のMarkdown記法がそのままテキストとして表示される |

---

## 非機能要件

| テストID | 対象要求ID | 前提条件 | 操作 | 期待結果 |
|----------|-----------|---------|------|---------|
| AT-NF01 | NF-01-01 | 100ノードを含むMarkdownファイルでマインドマップパネルが起動している | ノードのドラッグ・展開折りたたみ・名称変更などのインタラクション操作を繰り返す | 100ノード以下のマインドマップで描画遅延が体感されない（60fps相当で動作する） |
| AT-NF02 | NF-01-02 | マインドマップパネルが起動しており、編集操作を実施する | ブラウザの開発者ツールでパフォーマンスを計測しながら、D&Dや名称変更を実施する | Markdownへの反映が編集操作から500ms以内に完了する |
| AT-NF03 | NF-02-01 | マインドマップパネルで構造変更（D&D・追加・削除・移動）を実施した後 | Markdownファイルの内容（見出し以外の段落・コードブロック・フロントマター）を確認する | マインドマップからの編集操作によりMarkdown本文（見出し以外のテキスト）が失われていない |
| AT-NF04 | NF-02-02 | マインドマップパネルが起動している状態で意図的に拡張機能のエラーを引き起こす（パース不能なコードを入力する等） | VS Codeの他のウィンドウや機能（別のファイル・設定・ターミナル等）を操作する | 拡張機能のエラーがVS Code本体の動作に影響しない |
| AT-NF05 | NF-02-03 | 元ドキュメントの改行コードがCRLFのMarkdownファイルでマインドマップパネルが起動している | ノードのD&D・追加・削除等の構造編集操作を行い保存する | マインドマップ経由の書き込み後もファイルのEOLがCRLFのまま維持される。内部処理・base比較はLF正規化のままでよい（自動テスト `markdownRoundTrip.test.ts` の `NF-02-03: applyDocumentEol...`、`mindmapPanel.test.ts` の `NF-02-03: applyDocumentEdit...` で検証） |
| AT-NF06 | NF-03-01 | 拡張機能がインストールされている状態でパネルを開く | VS Code DevTools（`Help > Toggle Developer Tools`）のコンソールでCSPエラーを確認する | WebviewのCSP（コンテンツセキュリティポリシー）が適切に設定されており、コンソールにCSP違反エラーが出ない |
| AT-NF07 | NF-03-02 | 拡張機能がインストールされている状態 | 拡張機能のWebviewのHTMLソースを確認する | nonceベースのスクリプト実行制限が実装されており、すべての `<script>` タグに有効なnonceが付与されている |

---

## 変更履歴

| バージョン | 日付 | 変更内容 | 対応要求仕様バージョン |
|-----------|------|---------|----------------------|
| 1.6.0 | 2026-07-11 | MME-REQ-001-USDM v2.19.1 に追従。新設 R-14・R-20・R-21 のAT追加、R-01/R-02/R-06/R-10/R-11/R-12/R-13/R-18/R-19/NF-02 の欠落サブ要件AT追加（R-01-04, R-02-10, R-06-07〜10, R-10-04, R-11-09, R-12-07, R-12-10, R-13-08, R-13-12〜16, R-18-09〜12, R-19-06/07, NF-02-03）。R-05-02確認ダイアログ廃止・チェックボックス自動変換全廃・複数選択の同一親制限に合わせ AT-05-02・AT-13-08・AT-13-09・AT-13-11・AT-13-12・AT-18-01・AT-18-06 を改訂。列欠落だったAT-19-03を5列フォーマットへ修正 | MME-REQ-001-USDM v2.19.1 |
| 1.5.0 | 2026-06-26 | MME-REQ-001-USDM v2.15.0 に追従。Marketplace 再公開用のバージョン更新のみで、受け入れ条件の挙動変更なし | MME-REQ-001-USDM v2.15.0 |
| 1.3.0 | 2026-06-25 | MME-REQ-001-USDM v2.13.0 に追従。AT-07-05 を、ルート専用の紫系通常枠、選択時の間隔付きフォーカス外側リング、ライト／ダーク／高コントラストでの識別性を検証する内容へ更新 | MME-REQ-001-USDM v2.13.0 |
| 1.2.0 | 2026-06-25 | MME-REQ-001-USDM v2.12.0 に追従。AT-12-02 / AT-12-03 / AT-12-10 を画面方向ナビゲーションと展開/折りたたみ二段階動作へ更新し、ツリー更新後の本文選択の一意再束縛・曖昧時解除を検証する AT-12-11 を追加 | MME-REQ-001-USDM v2.12.0 |
| 1.1.2 | 2026-06-25 | MME-REQ-001-USDM v2.11.2 に追従。Issue #30 対応のロールバックに伴い、受け入れテスト内容は v2.11.0 と同一のまま版・履歴のみ整合した | MME-REQ-001-USDM v2.11.2 |
| 1.1.0 | 2026-06-25 | MME-REQ-001-USDM v2.11.0 に追従。ルートノードの新アクセント表現を検証する AT-07-05 を追加し、`↑/↓` の兄弟限定移動・ルート選択中 `←` の左ノード移動・本文項目の兄弟限定移動に合わせて AT-12-01 / AT-12-03 / AT-12-10 を更新 | MME-REQ-001-USDM v2.11.0 |
| 1.0.2 | 2026-06-25 | キーボードナビゲーション仕様更新に合わせて R-12 系の受け入れテスト定義を更新。AT-12-02 / AT-12-03 を左右配置非依存の左右キー規則へ改訂し、本文ノード選択中の左右キー規則を検証する AT-12-10（R-12-11）を追加 | MME-REQ-001-USDM v2.10.0 |
| 1.0.1 | 2026-06-20 | 純粋ロジックの自動テストを拡充（bodyItems / conflictDetection / collapsedPaths / markdownRoundTrip の境界値・異常系・複合フロントマターを追加）。AT-06-04 / AT-15-04 / AT-13-08 等の備考に自動テスト検証範囲を追記 | MME-REQ-001-USDM v2.6.0 |
| 1.0.0 | 2026-06-19 | 初版作成（5列フォーマット化・非機能要件テスト追加） | MME-REQ-001-USDM v2.6.0 |
