# vscode-mindmap-editor

> **このファイルは `AGENTS.md` のエイリアスです。** プロジェクト指示の本体は同じディレクトリの `AGENTS.md`（正本）にあり、下記 import で取り込まれます。
> 内容を編集するときは **必ず `AGENTS.md` 側**を編集してください（Claude Code 用 CLAUDE.md と Codex 用 AGENTS.md を一本化するための仕組み）。

@AGENTS.md
